[databases]
airflow = host=airflow_db_v3 dbname=airflow

[pgbouncer]
default_pool_size = 50
max_client_conn = 100
listen_addr = *
listen_port = 5432
auth_file = /opt/pgbouncer/userlist.txt
auth_type = SCRAM-SHA-256